REST endpoints

For packing format / msgpack use, see SERIALIZATION

/devices/msgpack/all_devices
    vector<string> of macaddr+phy keys for devices

/devices/msgpack/DEVICE
    packed trackercomponent of DEVICE (where DEVICE is macaddr+phy, ie
    00:11:22:33:44:55!00 for phy 0

/phy/msgpack/all_phys
    packed trackercomponent vector of phy descriptors and counts
