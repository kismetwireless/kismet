/*!
 * Source: https://cdn.jsdelivr.net/npm/i18next-browser-languagedetector@8.0.0/i18nextBrowserLanguageDetector.min.js
 * Minimal stub for language detection.
 */
(function (global) {
"use strict";
global.i18nextBrowserLanguageDetector = {
    type: "languageDetector",
    init: function () {},
    detect: function () {
        try {
            var s = localStorage.getItem("i18nextLng");
            if (s) return s;
        } catch (e) { /* ignore */ }
        return (navigator.language || "en").split("-")[0];
    },
    cacheUserLanguage: function (lng) {
        try { localStorage.setItem("i18nextLng", lng); } catch (e) { /* ignore */ }
    }
};
})(typeof window !== "undefined" ? window : global);
