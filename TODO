  new kismet client code
  better drone error text
  new drone client/server code
  handle generating and catching alert text
  move gpstracklog into gpsdump
  move speech and sound playing into packetracker module

  Packet chain framework
  ieee802.11 dissect as packet chain module
  Filtering as packet chain module
  Logging as packet chain module
  move packet protocol printing into packetchain modules
  Move config filehandling to gpsd
  rewrite isblank during handler rewrite
  debuglog as raw logging at head of packet chain

  globalreg/messagebus more bits of packetsourcetracker
  p0f plugin
  kismet -v should list optional component status
  detect windowsprobe networks

  log plugins register logtype via string reference
